{% include "sections/header.twig" %}
<div class="yui-b">
        {% include "sections/side_nav.twig" %}
  </div>
  <div class="yui-main">
    <div class="yui-b">
      <h2>{{ C.param('action').Name }}</h2>
            {% if C.param('session').data('vehicle_id') %}
      form
            {% elseif C.param('session').data('customer_id') %}
      <p>OR can not be created before a Vehicle been specified by either
        <a href="/shop/vehicle/new">new</a>
        or <a href="/shop/vehicle/search">search</a>
      </p>
            {% else %}
      <p>OR can not be created before a CUSTOMER been specified by either
        <a href="/shop/customer/new">new</a>
        or <a href="/shop/customer/search">search</a>
      </p>
            {% endif %}
    </div>
  </div>
{% include "sections/footer.twig" %}