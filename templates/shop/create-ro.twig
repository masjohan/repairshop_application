{% include "sections/header.twig" %}
<div class="yui-b">
        {% include "sections/side_nav.twig" %}
  </div>
  <div class="yui-main">
    <div class="yui-b">
      <h2>{{ C.param('action').Name }}</h2>
    <script>
    $(function() {
        $( "#accordion" ).accordion();
    });
    </script>
      <div id="accordion">
        <h3>Customer Search/Create</h3>
        <div>
            Customer
        </div>
        <h3>Vehicle search/create</h3>
        <div>
            Vehicle
        </div>
        <h3>Vehicle history</h3>
        <div>
            history
        </div>
        <h3>Repair order</h3>
        <div>
            ro
        </div>
      </div>
      {% if C.param('session').data('vehicle_id') %}
      {% elseif C.param('session').data('customer_id') %}
      {% else %}
      {% endif %}
    </div>
  </div>
{% include "sections/footer.twig" %}